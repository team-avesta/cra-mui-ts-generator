import React from 'react';
import Divider from '@material-ui/core/Divider';

interface IProps {}
const gridHeader: React.SFC<IProps> = props => {
	return (
		<React.Fragment>
			<Divider />
		</React.Fragment>
	);
};
export default gridHeader;
